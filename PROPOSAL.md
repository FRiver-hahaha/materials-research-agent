# 项目方案（Proposal）— 路线 A：构效关系发现

## 1. 问题定义

从材料科学文献出发，结合公开材料数据库，发现新的**「组分 / 结构 / 工艺 — 性质」**关联，
并给出可解释的科学解释与文献/数据库交叉验证。本方案以**钙钛矿太阳能电池材料**为示范领域，
目标是在「光学带隙 ≈ 1.3 eV（单结最佳）」与「相稳定性」之间找到最优组成，给出可复现的搜索流程。

## 2. 方法总览

```
文献 Gap（基础任务产出）
   │
   ▼
LLM 假设生成         → 候选构效关系（种子），如「调节 A 位半径使容忍因子接近 1」
   │
   ▼
搜索与优化（LLM 真正参与）
   ├─ 贝叶斯优化（高斯过程代理 + EI 采集）
   ├─ 遗传算法（组成空间进化）
   ├─ MCTS（离散组成决策树）
   └─ 符号回归（发现可解释构效方程）
   │  ★ LLM 在每步：提供种子 + 对中间候选做剪枝评估（容忍因子/稳定性经验法则）
   ▼
数据库交叉验证       → 与 Materials Project / OQMD / NOMAD 比对，判定新颖 or 已知
   │
   ▼
可解释输出           → 候选组成 + 预测性质 + 物理解释 + 证据链
```

## 3. 为什么 LLM 必须真正参与搜索

赛事明确要求：LLM 要参与搜索过程（生成候选假设当种子、评估中间结果合理性、引导剪枝），
"让它生成一段搜索代码就不管了，不算数"。本方案的实现：
- **种子生成**：LLM 基于文献证据提出初始候选与搜索方向（`route_a/hypothesis.py`）。
- **剪枝评估**：每个被搜索算法产出的候选组成，都经 LLM 依据容忍因子/稳定性经验法则评估是否相稳定
  （`route_a/explorer.py` 中 `llm.structured(kind="prune")`），不稳定候选被剪枝。
- **解释生成**：最终候选的物理解释由 LLM 撰写，连接搜索结果与第一性原理直觉。

## 4. 搜索空间与代理模型（示范）

- 决策变量：A 位有效半径 r_A ∈ [1.8, 2.4] Å、卤素 Br 比例 x ∈ [0, 1]。
- 物理代理：容忍因子 t = (r_A + r_X) / (√2 (r_B + r_X))；带隙 E_g 与稳定性 S 的经验关系。
- 目标：max  −(E_g − 1.3)² + 0.8·S（兼顾理想带隙与相稳定性）。
- 真实参赛时，代理模型替换为 **Materials Project / OQMD 查询 + DFT/MLP 计算**，搜索在真实性质曲面上进行。

## 5. 交叉验证与可解释性

- 候选预测带隙与公开数据库已知值比对（容差 0.05 eV）：命中 → 标注"已知构效关系"；未命中 → 标注"新颖"。
- 符号回归产出可解释方程（如 E_g ≈ f(t, x)），比黑箱模型更符合赛事对可解释性的要求。

## 6. 实施计划（初赛 → 复赛）

| 阶段 | 内容 | 交付 |
|------|------|------|
| 初赛 | 系统架构 + 基础任务 + 路线A 方案与示范实现 | 调研报告 + 系统说明 + 本 Proposal |
| 复赛 | 接入 Sciverse/MinerU/MP 真实数据，跑通可复现代码 | 代码 + 技术报告（论文形式） |
| 决赛 | 现场路演，展示端到端发现流程 | 路演材料 |

## 7. 预期产出与价值判断

- 一份"带隙—稳定性"帕累托前沿上的新材料候选清单，附文献证据链与数据库交叉验证。
- 可解释构效方程，可作为后续 DFT 验证或实验合成的起点。
- 系统架构可复用于其他材料体系（只需替换语料与数据库适配器）。

## 8. 风险与对策

- **检索覆盖不足** → 双来源（Sciverse + Sci-Base RAG）+ 相关度兜底，避免漏检。
- **LLM 幻觉** → 所有数值结论绑定 Evidence 对象，核验环节强制溯源。
- **搜索陷入局部最优** → 多算法可切换（BO/GA/MCTS），LLM 提供多起点种子。
